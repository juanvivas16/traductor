#include "asdasd"
#include "asdf.h"
#include <hola.h>
#include <asdf>

int main()
{
  int X;
  scanf("%d", &X);

  int i = 2;
  int j = 4;
  int contador = 0;
  int mod;

  return 23;

  if (a == b)
    a = b;
   else
    c = d;

  if (a == b)
  {
    if (d==c)
      ad=dc;

    int i = 2;
    int j = 4;

    while ( c <= 4)
      int f = 6;

      if (a == b)
      {
        int f = 6;
      }
      do {
        termino = a + b + c;
        printf(", %d", termino);
        a = b;
        b = c;
        c = termino;
        i += 1;
        return 34;

      } while(i <= N);
  }
  else
  {
    int X;
    scanf("%d", &X);
    return (df);
  }

  do {
    termino = a + b + c;
    printf(", %d", termino);
    a = b;
    b = c;
    c = termino;
    i += 1;

    if (d==c)
      ad=dc;

    int i = 2;
    int j = 4;

    while ( c <= 4)
      int f = 6;

      if (a == b)
      {
        int f = 6;
        return d;
      }

  } while(i <= N);


  return 0;

}
