#include <stdlib.h>
#include <stdio.h>

int main()
{
  int X;
  scanf("%d", &X);

  int i = 2;
  int j = 4;
  int contador = 0;
  int mod;

  if (a == b)
    a = b;
   else
    c = d;

  if (a == b)
  {
    if (d==c)
      ad=dc;

    int i = 2;
    int j = 4;

    while ( c <= 4)
      int f = 6;

      if (a == b)
      {
        int f = 6;
      }
    int mod;
  }
  else
  {
    int X;
    scanf("%d", &X);
  }

  do {
    termino = a + b + c;
    printf(", %d", termino);
    a = b;
    b = c;
    c = termino;
    i += 1;

  } while(i <= N);


  return 0;

}
